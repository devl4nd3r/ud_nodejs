const superheroe = "Batman";
const nombre = "Bruce Wayne";

const concat = nombre + " alias:" + superheroe;
const templateString = `${nombre} alias:${superheroe}`;
console.log(concat);
console.log(templateString);
